<Window x:Class="CsharpBeadando1.AdminSelectPatient"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Páciens keresés" MaxHeight="200" MaxWidth="400" MinHeight="200" MinWidth="400">
    <Grid>
        <StackPanel Margin="20" VerticalAlignment="Center">
            <DockPanel HorizontalAlignment="Left">
                <Label Content="Név" Width="150" />
                <TextBox Name="Nev" Width="150" />
            </DockPanel>
            <DockPanel HorizontalAlignment="Left">
                <Label Content="Szobaszám" Width="150" />
                <TextBox Name="Szobaszam" Width="150" />
            </DockPanel>
            <DockPanel HorizontalAlignment="Center">
                <Button Click="OnSelectButtonClick" Content="Keres" Margin="10" Width="90" />
                <Button Click="OnCancelButtonClick" Content="Mégse" Margin="10" Width="90" />
            </DockPanel>
        </StackPanel>
    </Grid>
</Window>